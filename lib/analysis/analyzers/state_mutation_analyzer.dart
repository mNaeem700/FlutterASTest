import 'package:flutterastest/parser/models/parser_result.dart';
import '../models/state_mutation.dart';

class StateMutationAnalyzer {
  const StateMutationAnalyzer();

  List<StateMutation> analyse(ParserResult parserResult) {
    // AST traversal logic will be added in the next sub-step
    return [];
  }
}
